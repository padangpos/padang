import { NextResponse } from 'next/server';
import { createStockTransfer } from '@/lib/operations/server-operations';
export async function POST(request: Request) { try { const body = await request.json(); const input = { fromBranchId: String(body.fromBranchId || ''), toBranchId: String(body.toBranchId || ''), productId: String(body.productId || ''), quantity: Number(body.quantity), idempotencyKey: String(body.idempotencyKey || crypto.randomUUID()), note: body.note ? String(body.note) : undefined }; if (!input.fromBranchId || !input.toBranchId || !input.productId || !Number.isFinite(input.quantity) || input.quantity <= 0) return NextResponse.json({ error: 'ข้อมูลโอนสต็อกไม่ถูกต้อง' }, { status: 400 }); return NextResponse.json({ transfer: await createStockTransfer(input) }, { status: 201 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : 'โอนสต็อกไม่สำเร็จ' }, { status: 500 }); } }
